@model IEnumerable<FullWeb.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClothingType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClothingColor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClothingSize)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClothingPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fabric)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClothingType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClothingColor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClothingSize)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClothingPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fabric)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
